# Database Configuration
PG_HOSTNAME=localhost
PG_PORT=5432
PG_DATABASE=athenai
PG_USERNAME=postgres
PG_PASSWORD=your_password_here

# Application Environment
# Options: dev (development) | prod (production)
# dev: Returns detailed error information including SQL errors
# prod: Returns only error codes for security
APP_ENV=dev

# Server Configuration
PORT=8080

# LLM API Configuration
LLM_ENDPOINT=https://api-inference.huggingface.co/models/your_model
API_TOKEN=your_api_token_here